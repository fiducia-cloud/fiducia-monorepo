# direnv config: auto-loads the Nix dev shell and local env/.prod.env on cd.
watch_file .nix/flake.nix
watch_file .nix/flake.lock
watch_file env/.prod.env
if [ -f env/.prod.env ]; then
  dotenv env/.prod.env
fi
use flake ./.nix
